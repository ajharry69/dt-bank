server:
  port: 8100
  forward-headers-strategy: framework
spring:
  threads:
    virtual:
      enabled: true
springdoc:
  paths-to-match: /api/**
  api-docs:
    path: /card-service/v3/api-docs
  swagger-ui:
    path: /card-service/swagger-ui.html