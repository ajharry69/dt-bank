#! gateway-deployment.yaml
# Generated code, do not edit
apiVersion: apps/v1
kind: Deployment
metadata:
    name: gateway
    namespace: dt-bank
    labels:
        com.docker.compose.project: dt-bank
        com.docker.compose.service: gateway
spec:
    replicas: 1
    selector:
        matchLabels:
            com.docker.compose.project: dt-bank
            com.docker.compose.service: gateway
    strategy:
        type: Recreate
    template:
        metadata:
            labels:
                com.docker.compose.project: dt-bank
                com.docker.compose.service: gateway
                com.docker.compose.network.default: "true"
        spec:
            containers:
                - name: gateway
                  image: ghcr.io/ajharry69/gateway:compose
                  imagePullPolicy: IfNotPresent
                  env:
                    - name: CONFIG_SERVER_URL
                      value: "http://config-server:8080"
                    - name: SPRING_PROFILES_ACTIVE
                      value: "compose"
                  ports:
                    - name: gateway-8080
                      containerPort: 8080
